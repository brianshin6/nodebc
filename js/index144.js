const user = {username: 'batman',
              password: 'Alfred1960ROCKS!'}

console.log(user.username, user.password)

console.log(user.username.toUpperCase(), user.password.slice(0, 6) + ' ' + user.password.slice(10))