let height = 10;
let width = 4;
let perimeter = `A rectangle has 4 sides, therefore to find the perimter of a rectangle, you sum the height and width and multiply by 2. \n To find the area of the rectangle in question, we add the height of ` + height + ' by the width of ' + width + ', and multiply by 2. Resulting in ' + (height + width) * 2
let area = `A rectangle has 4 sides, therefore to find the area of a rectangle, you multiply the height by the width. \n To find the area of the rectangle in question, we multiply the height of ` + height + ' by the width of ' + width + ', resulting in ' + (height * width)
console.log(perimeter)
console.log(area)